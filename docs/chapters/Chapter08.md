# Chapter 08. SSML, Speech Marks 확장

## 학습 목표
- 단순 텍스트 외에 SSML 입력을 활용한다.
- 자막/싱크 기능을 위해 speech marks를 이해한다.

## 확장 아이디어
- FE에서 `text`/`ssml` 전환 토글 제공.
- `/marks` 또는 별도 Lambda에서 타이밍 JSON 반환.
- WebVTT로 변환해 자막 싱크 플레이 구현.
